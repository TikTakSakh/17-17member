services:
  bot:
    build: .
    container_name: bar1717-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./service_account.json:/app/service_account.json:ro
      - bot-data:/app/data
      - ./logs:/app/logs
      - ./history:/app/history
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  bot-data:
